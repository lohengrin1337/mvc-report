<div class="gameboard">
    {% for row in range(1, 5) %}
        <div class="row">
            {% for col in range(1, 5) %}
                {% set slot = row ~ col %}

                {% if game.board[slot] %}
                    <div class="slot {{ game.board[slot] }}">
                {% else %}
                    <div class="slot">
                {% endif %}

                {% if row == 5 %}
                    <span class="hand-score-col header-sm">{{ game.handScores["col" ~ col] }}</span>
                {% endif %}

                </div>

            {% endfor %}
            <span class="hand-score-row header-sm">{{ game.handScores["row" ~ row] }}</span>
        </div>
    {% endfor %}

    <form id="cpu_play_form" action="{{ path('proj_game_play') }}" method="get"></form>
</div>
