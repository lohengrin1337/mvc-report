<div class="deck-and-buttons">
    {% for row in range(1, 5) %}
        <div class="row">
            {% for col in range(1, 5) %}
                <div id="{{ row ~ "-" ~ col }}" class="slot"></div>
            {% endfor %}
        </div>
    {% endfor %}
</div>

