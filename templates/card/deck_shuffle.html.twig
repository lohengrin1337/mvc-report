{% extends "base.html.twig" %}

{% block body %}
    <main class="main text-container">
        <h1>{{ pageTitle }}</h1>

        <div class="cards">
            {% for card in allCards %}
                <span class="card">{{ card }}</span>
            {% endfor %}
        </div>

    </main>
{% endblock %}
