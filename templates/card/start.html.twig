{% extends "base.html.twig" %}

{% block body %}
    <main class="main text-container">
        <h1>{{ pageTitle }}</h1>

        {{ include('card/content/start.md')|markdown_to_html }}

        <a href="{{ asset('img/UML-mvc-kmom02.svg') }}">
            <img class="img" src="{{ asset('img/UML-mvc-kmom02.svg') }}" alt="UML klassdiagram">
        </a>

    </main>

    <div class="aside aside-top">
        {{ include('card/_nav.html.twig') }}
    </div>
{% endblock %}
